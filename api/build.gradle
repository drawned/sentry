dependencies {}

tasks.withType(JavaCompile).configureEach {
    options.encoding = 'UTF-8'
    options.release.set(21)
}

tasks {
    jar {
        archiveFileName.set("Sentry-${project.version}-api.jar")
    }
}